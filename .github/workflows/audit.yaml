name: Code audit

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  audit:
    name: Code audit
    runs-on: ubuntu-latest

    steps:
      - name: (>) Git checkout
        uses: actions/checkout@v4

      - name: (>) Check node_modules is ignored
        uses: dkershner6/gitignore-parser@6a5fba55e4a7eaafe547c7697a5147e98d6df33e # v1
        with:
          must_deny: "node_modules"

      - name: (>) Enable Corepack
        run: corepack enable

      - name: (>) Set up Node.js 22.x
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: "pnpm"

      - name: (>) Install dependencies
        run: pnpm install --frozen-lockfile

      - name: (>) ESLint and Prettier validation
        run: pnpm lint
